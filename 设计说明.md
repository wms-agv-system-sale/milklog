# 奶量日记 · 设计说明

## 1. 目标

用 iPhone 摄像头**自动读出奶瓶里的奶量**，记录下来并给出日 / 周 / 月折线统计，帮助观察宝宝一段时间的奶量变化。要求：完全离线、可手动修正数值。

## 2. 关键技术判断

「拍一张照片就知道有多少毫升」在物理上有两种做法：

| 做法 | 说明 | 结论 |
| --- | --- | --- |
| A. 靠瓶子上的印刷刻度 OCR | 读数字反推容量 | 误差大：数字只是刻度，不代表液面高度；瓶身弧度、标签遮挡都会失败 |
| B. 测液面高度 + 标定 | 找液面在画面里的位置，用「高度 ↔ 毫升」的换算关系求出体积 | **采用**：稳定、可解释、可标定 |

B 方案的前提是：**同一台手机、同样的机位**。所以产品设计上用「一次性标定 + 固定机位」来换准确度，这也是目前所有拍照测量类 App 的通行做法。

## 3. 识别算法（LiquidDetector）

输入是相机 YUV 视频帧（`420f`），只在**奶瓶所在的竖直条带**（band，默认画面中间 32%~68%）里分析：

1. **逐行统计**（对 Y 平面取亮度，对 CbCr 平面取色度）：
   - `rowLuma[y]`：该行平均亮度；
   - `rowMilky[y]`：该行「像牛奶」的像素比例 —— 亮度 > 140 且 |Cb-128|、|Cr-128| < 24（即偏白、无明显颜色）。
2. **找分界线**：在允许的液面范围内扫描每个候选行 y：
   - `below` = y 下方 10 行平均 milky 比例（应接近 1）；
   - `above` = y 上方 10 行平均 milky 比例（应接近 0，因为上方是空气/背景）；
   - `contrast = below - above`，并要求 `below > 0.45`、`contrast > 0.12`；
   - `jump` = 液面处亮度跳变（反光/折射会产生明显边缘）；
   - `score = contrast × (0.7 + min(1, jump/26))`，取最大值。
3. **亚像素修正**：用相邻三点的抛物线插值把位置精确到 0.5 像素以内。
4. **换算**：`毫升 = BottleProfile.volume(forY:)`（分段线性插值，范围外按最近一段斜率外推并限幅）。

时间维度上再做一次**中值平滑**（最近 6 帧取中位数），并要求连续稳定（极差 < 0.015）才判定为「读数稳定」，避免数字乱跳。

### 标定模型

```
CalibrationPoint { volumeML, y }     // y 为画面归一化高度，0 = 顶部
BottleProfile    { name, points[], bandLeft, bandRight }
```

用户在已知奶量（如 60 / 120 / 180 ml）下各记录一次液面高度，即得到若干 (y, ml) 样本，任意高度通过分段线性插值得到体积。记录点越多越准，最少 2 个。同一奶瓶只需标定一次。

### 误差来源与对策

| 误差来源 | 对策 |
| --- | --- |
| 手机位置变化 | 标定页面提示固定机位；结果可手动修改；重新标定成本很低（1 分钟） |
| 背景干扰（白墙/白桌布） | 用色度区分「牛奶」与「白背景」较难，改用取样条带 + 亮度跳变双重约束；用户可收窄取样条带 |
| 反光、阴影 | 提供补光（闪光灯）开关；score 里用跳变强度加权，反光边缘反而有利 |
| 泡沫、挂壁 | 中值平滑 + 稳定性判据 |
| 奶量低于最低标定点 | 外推并限幅，同时界面提示 |

## 4. 数据与存储

- `AppStore` 把全部数据序列化成 `Documents/MilkLog/data.json`，照片存 `Documents/MilkLog/Photos/*.jpg`。
- 无数据库依赖、无网络代码。删除 App 即删除全部数据，因此提供 JSON 导出备份。
- 数据结构：`FeedRecord { id, date, volumeML, source, note, photoName, confidence }`。

## 5. 界面

| 页面 | 作用 |
| --- | --- |
| 记录 | 实时相机 + 液面参考线 + 大号读数 + −5/+5 微调 + 保存；未标定时引导去标定；无相机权限时给出指引 |
| 标定 | 相机预览 + 已记录标定点可视化 + 自动识别 / 点选液面 + 取样区域调节 |
| 统计 | 日 / 周 / 月切换，折线 + 面积图（Swift Charts），平均 / 次数 / 峰值指标，下面是按天分组的明细，可编辑删除 |
| 设置 | 多奶瓶管理与标定、每日目标、照片留存、导出、清空、使用说明 |

## 6. 技术选型

- SwiftUI + Swift Charts（系统内置，离线可用，无需任何第三方依赖）
- AVFoundation 采集 + CoreVideo 平面数据直接分析（不走 GPU，功耗低、延迟小）
- Vision（本地文字识别，备用：读瓶身刻度数字）
- 最低 iOS 17，仅 iPhone、仅竖屏

## 7. 后续可以做的增强

1. 用 OCR 读瓶身刻度，自动定位标定点，进一步减少手动步骤。
2. 记录「没喝完」的剩余量，算出真实摄入量。
3. 用多帧立体/参考物（例如固定尺寸的参照卡）解除「固定机位」限制。
4. 数据导出为 CSV、生成可分享的周报图片。
5. 双胞胎 / 多宝宝档案切换。